{
  "name": "rotating-proxy-gateway",
  "version": "1.0.0",
  "description": "Gateway that rotates proxied requests across a pool of upstream proxy nodes",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "node-fetch": "^2.6.12",
    "express-rate-limit": "^6.7.0"
  },
  "engines": {
    "node": ">=14"
  }
}
